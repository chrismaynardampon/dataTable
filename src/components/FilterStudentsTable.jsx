/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import React from 'react';

const FilterStudentsTable = (props) => {
const {children} = props;
  return (
    <div>
      {children}
    </div>
  )
}

export default FilterStudentsTable
